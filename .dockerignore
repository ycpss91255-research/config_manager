# 讓建置脈絡只留下映像真正需要的東西。底下每一項不是影響不了映像，就是不該進到
# 映像裡。

.git/
.github/
**/__pycache__/
**/*.pyc
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 設計素材，不是建置輸入。
doc/
figures/
tools/
*.pdf

# 操作者自有的覆寫設定，永不烤進映像（`.local` 字尾標記的是一整族：
# `.setup.conf.local`、`.env.local`……）。
*.local
.env

# 衍生產物。
coverage/
htmlcov/
.coverage
