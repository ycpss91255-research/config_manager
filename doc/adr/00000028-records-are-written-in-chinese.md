# 紀錄一律以中文書寫，只有機器讀的 type 與 scope 維持英文

> 服務：機制，無對應不變式

- **Date:** 2026-09-04
- **Status:** Accepted
- **Relates to:** ADR-00000025（commit 慣例取樣自 base）、ADR-00000016（對齊模板 main）

## Context

**為什麼這條掛在「機制」而不掛在任何不變式上。** 九條不變式逐條看過，沒有一條說得到
「紀錄用什麼語言書寫」：不變式 8 管的是 `uid`／`name`／`hostname`／`target`／`format`／
`permissions` **從哪裡取得**，與書寫語言無關；不變式 6 管的是同一份資料不得有兩個 owner，
而中文與英文不是同一份資料的兩份副本；不變式 9 管的是「可推導的東西不進文件」，語言不是
推導得出來的。**唯一構得上邊的是不變式 2**——「人會讀到的執行期輸出」這一段確實服務它：
讀者看不懂的錯誤訊息，形式上大聲、效果上靜默，與設計 §0.4 說的「『格式錯誤』等同於沒有
訊息」是同一種失效。**但那只涵蓋本決策九個適用範圍裡的一個**，其餘八個（commit、PR、
issue、milestone、ADR、專案文件、CLAUDE.md、程式碼註解、測試名稱）都是寫下來的紀錄，
不變式 2 對它們沒有話說。把一條只解釋得了九分之一的不變式寫進回指欄，就是下一個讀者
會據以誤引的東西——正是 §0.5 設這一欄要防的。

本決策真正的驅動因素是**讀者是誰**（見下段），那是一個關於這個 repo 的事實，不是產品
性質，所以它落在 §0.5 允許的第三種形式。同類的決策在本目錄已有三份先例：
ADR-00000001（ADR 格式）、ADR-00000007（變更紀錄的 commit 慣例）、ADR-00000010
（REST 風格），三份都寫「機制，無對應不變式」。

本 repo 的紀錄語言長成了混的：issue、milestone、README、`doc/test/TEST.md`、
ADR 內文用中文；commit 訊息、PR 標題、CLAUDE.md、程式碼註解、ADR 標題用英文。
沒有任何一條規則說明分界在哪，所以每個人每次都要重新判斷，而判斷結果不一致。

成因可追溯：ADR-00000025 決定 commit 慣例「取樣自 base 的實際 commit」，
而 base 的 commit 是英文的——於是語言跟著格式一起被抄了過來，**但那不是同一件事**。
base 用英文，是因為它的讀者是取用該模板的任何人；本 repo 不是 base 的 downstream
（ADR-00000016 明確決定不引入模板），它的讀者就是這裡的維護者。

## Decision

**紀錄一律以中文書寫。** 唯一的例外是 `type` 與 `scope`——那兩個是給工具讀的識別碼，
維持 base 定義的形式。

```
feat(core): 清單檔載入時攔截未知欄位
```

適用範圍：commit 訊息、PR 標題與內文、issue、milestone、ADR（含標題）、
專案文件、CLAUDE.md、程式碼註解、**測試名稱**，以及**人會讀到的執行期輸出**
——`usage()` 的說明文字、錯誤訊息、CLI 與網頁的介面文案。

**執行期輸出這一項是 2026-09-04 補上的（#106）。** 原本的清單只涵蓋「寫下來的紀錄」，
沒有涵蓋「程式印出來的字」，於是 27 支腳本的 `usage()` 與各處錯誤訊息落在規則之外。
那個空缺是漏掉而非刻意：這條 ADR 的理由是「沒有英文讀者」，而那個理由對錯誤訊息
只有更成立——出錯的當下最需要看得懂。網頁的介面文案本來就是中文
（`CONTEXT.md`「避免使用的說法」），執行期輸出跟著同一條線才一致。

**執行期輸出裡維持英文的**：旗標名稱（`--all`、`--lint`）、環境變數名、路徑、
指令、型別與函式名。判準與 type／scope 相同——**那些是識別碼，人照著打進去的**，
翻譯它們會讓訊息不可執行。

`script/lint_commit.sh` 檢查主旨含中文字，**fail**——依 §0.6，無法自動檢查的規範等於不存在。
既有歷史不在檢查範圍內（只看 `origin/main..HEAD`），重寫它意味著對別人手上的分支強制推送。

ADR-00000025 的其餘部分不受影響：type 白名單、scope、格式、長度不設限、
規則取樣自 base——那些都是**格式**決策，與語言正交。本 ADR 只推翻其中隱含的語言假設。

## Alternatives

- **維持混用，並寫下分界。** 例如「對齊 base 的用英文、給人讀的用中文」。
  問題是 commit 訊息同時是兩者：它既要與 base 的慣例可比對（格式），
  也是給這裡的人讀的（內容）。把它整條劃到英文那邊，是讓格式的理由決定了內容的語言。
- **全部改英文。** 與現況距離更遠（issue、milestone、README、ADR 內文都要翻），
  且沒有對應的收益——沒有英文讀者。
- **不設規則，各憑判斷。** 這正是先前的狀態，結果是同一份 repo 裡兩種語言交錯，
  而且無法檢查。

## Consequences

- 既有的英文紀錄不回頭改寫 commit 歷史。**活的文件**（CLAUDE.md、程式碼註解、
  ADR 標題）逐步轉換，各自獨立的變更，不與功能改動混在同一個 diff 裡。
- 與 base 的 commit 並排時，只有 `type(scope)` 對得上。這是刻意接受的：
  本 repo 不是它的 downstream，可比對性的價值僅止於格式。
- 若日後真的引入模板並成為 downstream，本決策應重新檢視——屆時 repo 會有
  上游讀者，語言的權衡就不同了。

---

**Amendment (#125, 2026-09-05):** 決策未變，回指改了。原本寫的是
「不變式 8（身分與命名一次解析，來自檔案）」，那條不成立——不變式 8 管的是身分欄位
從哪裡取得，與紀錄的書寫語言無關。改為「機制，無對應不變式」，判準寫在 `## Context`
開頭。**這個錯誤 `lint_adr` 抓不到，而且抓不到是對的**：它檢查的是那一行存在
（`script/lint_adr.sh` 的 `grep -q '^> 服務'`），要檢查回指成不成立需要理解決策內容，
設計 §0.5 的方塊也只承諾結構檢查。這道缺口由 review 補，記在 `doc/adr/README.md`。
